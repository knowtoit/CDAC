package com.demo;
import java.util.Scanner;

public class Test2DArray {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int [][] arr = new int[3][3];
		
		Array2DService.acceptData(arr);
		Array2DService.displayData(arr);
		
		int choice = 0;
		do {
			System.out.println("1. find max 2. find min 3. "
					+ "find sum 4. find sum rowwise  5.find "
					+ "sum columnswise "
					+"6. display  7. find rowwise maximum "
					+ "8.find columnwise maximum 9. exit  choice:");
			
			
			
			choice = sc.nextInt();
			
			switch(choice) {
			case 1->{
				int max = Array2DService.findMax(arr);
				System.out.println("Maximum: " + max);
			}
			case 2->{
				int min = Array2DService.findMin(arr);
				System.out.println("Minimum: " + min);
			}
			case 3->{
				int sum = Array2DService.findSum(arr);
				System.out.println("Sum");
			}
			
			case 4->{
				int[] sumarr = Array2DService.findSumRowWise(arr);
				System.out.println("Rowwise sum: " + sumarr);
			}
			case 5->{
				
			}
			
			case 6->{
				
			}
			case 7->{
				
			}
			case 8 ->{
				
			}
			case 9->{
				
			}
			
			default->{
				System.out.println("Wrong choice");
			}
			
			}
		}
		while(choice !=9);
		
		sc.close();
	}
}
